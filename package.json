{
  "scripts": {
    "dev": "concurrently -n web,ext -c blue,green \"npm run dev --prefix younote-web\" \"npm run dev --prefix younote-ex\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}